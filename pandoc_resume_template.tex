$if(fontsize)$
\documentclass[$fontsize$,letterpaper]{article}
$else$
\documentclass[10pt,letterpaper]{article}
$endif$

\usepackage{iftex}
$if(engine)$$else$
\newcommand{\texengine}{pdftex}
$endif$

\ifPDFTeX
    \input{glyphtounicode}
    \pdfgentounicode=1
    \usepackage[T1]{fontenc}
    \usepackage[utf8]{inputenc}
    \usepackage{lmodern}
\else
    \ifLuaTeX
        \usepackage{fontspec}
    \else
        \usepackage{fontspec}
    \fi
$if(mainfont)$
    \setmainfont{$mainfont$}
$endif$
$if(sansfont)$
    \setsansfont{$sansfont$}
$endif$
$if(monofont)$
    \setmonofont{$monofont$}
$endif$
\fi

\usepackage{geometry}
\geometry{
    ignoreheadfoot,
    top=$if(margin_top)$$margin_top$$else$1cm$endif$,
    bottom=$if(margin_bottom)$$margin_bottom$$else$1cm$endif$,
    left=$if(margin_left)$$margin_left$$else$2cm$endif$,
    right=$if(margin_right)$$margin_right$$else$2cm$endif$,
    footskip=$if(margin_footskip)$$margin_footskip$$else$1cm$endif$
}

\usepackage{titlesec}
\usepackage[dvipsnames]{xcolor}
$if(primary_color)$
\definecolor{primaryColor}{HTML}{$primary_color$}
$else$
\definecolor{primaryColor}{RGB}{0,0,0}
$endif$
\usepackage{enumitem}
\usepackage[unicode=true]{hyperref}
\hypersetup{
    colorlinks=true,
    urlcolor=primaryColor,
$if(pdf_title)$    pdftitle={$pdf_title$},
$endif$$if(pdf_author)$    pdfauthor={$pdf_author$},
$endif$$if(pdf_subject)$    pdfsubject={$pdf_subject$},
$endif$$if(pdf_keywords)$    pdfkeywords={$pdf_keywords$},
$endif$
}
\usepackage{changepage}
\usepackage{paracol}
\usepackage{needspace}
\usepackage{setspace}

$if(linespread)$
\setstretch{$linespread$}
$else$
\setstretch{1.0}
$endif$

\raggedright
\AtBeginEnvironment{adjustwidth}{\partopsep0pt}
\pagestyle{empty}
\setcounter{secnumdepth}{0}
\setlength{\parindent}{0pt}
\setlength{\topskip}{0pt}
\setlength{\columnsep}{0.15cm}
\setlength{\parskip}{$if(paragraph_spacing)$$paragraph_spacing$$else$0pt$endif$}
\pagenumbering{gobble}

$if(section_font_size)$
\newcommand{\SectionFontSize}{\fontsize{$section_font_size$}{$section_font_size$}\selectfont}
$else$
\newcommand{\SectionFontSize}{\large}
$endif$

$if(section_rule_thickness)$
\titleformat{\section}{\needspace{4\baselineskip}\bfseries\SectionFontSize}{}{0pt}{}[\vspace{1pt}\titlerule[$section_rule_thickness$]]
$else$
\titleformat{\section}{\needspace{4\baselineskip}\bfseries\SectionFontSize}{}{0pt}{}[\vspace{1pt}\titlerule]
$endif$

\titlespacing{\section}{$if(section_left_margin)$$section_left_margin$$else$-1pt$endif$}{$if(section_space_before)$$section_space_before$$else$0.3cm$endif$}{$if(section_space_after)$$section_space_after$$else$0.2cm$endif$}

\renewcommand\labelitemi{$\vcenter{\hbox{\small$\bullet$}}$}

\newenvironment{onecolentry}{\begin{adjustwidth}{0.00001cm}{0.00001cm}}{\end{adjustwidth}}
\newenvironment{twocolentry}[2][]{\onecolentry\def\secondColumn{#2}\setcolumnwidth{\fill,4.5cm}\begin{paracol}{2}}{\switchcolumn\raggedleft\secondColumn\end{paracol}\endonecolentry}
\newenvironment{header}{\setlength{\topsep}{0pt}\par\kern\topsep\centering\linespread{$if(header_linespread)$$header_linespread$$else$1.5$endif$}}{\par\kern\topsep}

\let\hrefWithoutArrow\href

\newcommand{\AND}{\unskip\cleaders\copy\ANDbox\hskip\wd\ANDbox\ignorespaces}
\newsavebox\ANDbox
\sbox\ANDbox{$|$}
\newcommand{\contactseparator}{\kern5pt\AND\kern5pt}

\begin{document}

$if(hide_header)$
$else$
\begin{header}
    \fontsize{$if(header_name_font_size)$$header_name_font_size$$else$25pt$endif$}{$if(header_name_font_size)$$header_name_font_size$$else$25pt$endif$}\selectfont $if(name)$$name$$else$Nombre del Candidato$endif$

$if(headline)$
\vspace{4pt}
\fontsize{$if(headline_font_size)$$headline_font_size$$else$12pt$endif$}{$if(headline_font_size)$$headline_font_size$$else$12pt$endif$}\selectfont\itshape
$headline$
$endif$

$if(location)$
\vspace{5pt}
\normalsize
\mbox{$location$}$if(contacts)$\contactseparator$endif$
$endif$
$if(contacts)$
$if(location)$
$else$
\vspace{5pt}
\normalsize
$endif$
$for(contacts)$
$if(contacts.url)$\mbox{\hrefWithoutArrow{$contacts.url$}{$contacts.label$}}$else$\mbox{$contacts.label$}$endif$$sep$\contactseparator$endfor$
$endif$
\end{header}

\vspace{$if(header_body_spacing)$$header_body_spacing$$else$\dimexpr5pt-0.3cm\relax$endif$}
$endif$

$if(contact_section_title)$
\section{$contact_section_title$}
$if(contact_section)$
\begin{itemize}[topsep=0pt,itemsep=0pt,parsep=0pt,partopsep=0pt,leftmargin=*]
$for(contact_section)$    \item $contact_section$
$endfor$
\end{itemize}
$endif$
$endif$

$body$

\end{document}
